# Build artifacts
target/
# Exception: Allow pre-built JOLT Atlas binary for faster Render builds
!jolt-atlas-fork/target/release/examples/proof_json_output
*.log
*.tmp

# Node modules
node_modules/
package-lock.json

# Rust build
# Note: Cargo.lock is NOT ignored for jolt-atlas-fork because it's a binary/executable
# For binaries, Cargo.lock should be committed to ensure reproducible builds
# See: https://doc.rust-lang.org/cargo/faq.html#why-do-binaries-have-cargolock-in-version-control-but-not-libraries
*.rlib
*.so
*.dylib

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Removed/redundant directories
api-server/
fresh-build/
jolt-atlas/
jolt-napi/
jolt-prover/
dory-fork/
zkx402-agent-auth/

# Cargo cache artifacts
.cargo-home/
